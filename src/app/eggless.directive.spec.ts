import { EgglessDirective } from './eggless.directive';

describe('EgglessDirective', () => {
  it('should create an instance', () => {
    const directive = new EgglessDirective();
    expect(directive).toBeTruthy();
  });
});
