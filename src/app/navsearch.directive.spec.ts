import { NavsearchDirective } from './navsearch.directive';

describe('NavsearchDirective', () => {
  it('should create an instance', () => {
    const directive = new NavsearchDirective();
    expect(directive).toBeTruthy();
  });
});
