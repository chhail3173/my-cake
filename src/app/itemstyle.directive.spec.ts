import { ItemstyleDirective } from './itemstyle.directive';

describe('ItemstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemstyleDirective();
    expect(directive).toBeTruthy();
  });
});
